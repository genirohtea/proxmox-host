---
# Total host memory, in GiB, to reserve as 2 MiB hugepages. Zero removes any
# reservation previously managed by this role.
hugepages_2mb_gib: 0

# Proxmox installations use either GRUB or systemd-boot.
bootloader_type: unknown

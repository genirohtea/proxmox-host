---
# vars file for intel_graphics
#
# Intentionally empty. All tunables live in defaults/main.yml so that inventory
# host_vars can override them per host. Role vars (this file) sit ABOVE inventory
# in Ansible's precedence, so defining host-specific values here would silently
# prevent hosts from overriding them.
